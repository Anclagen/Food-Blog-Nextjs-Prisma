services:
  db:
    image: postgres:15
    container_name: pg_dev_shopping_planner
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: shopping-planner-blog
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
